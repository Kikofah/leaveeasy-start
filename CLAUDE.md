# CLAUDE.md — LeaveEasy

รายละเอียดทั้งหมดอยู่ใน `leaveeasy-spec.md` — ไฟล์นี้สรุปเฉพาะสิ่งที่ต้องรู้ก่อนแก้โค้ดทุกครั้ง

## 1. โฟลเดอร์ (collection) ทั้งหมดใน Firestore

| โฟลเดอร์ | ระดับ | เก็บอะไร |
|---|---|---|
| `users` | บนสุด | ผู้ใช้ (`name`, `email`, `role`) |
| `leaveTypes` | บนสุด | ประเภทการลา |
| `leaveRequests` | บนสุด | ใบขอลาแต่ละใบ |
| `approvals` | ย่อย (ซ้อนใต้ไฟล์ใบลาแต่ละใบใน `leaveRequests`) | ความเห็นการอนุมัติของใบลานั้น ๆ |

ชื่อโฟลเดอร์เขียนติดกันแบบ camelCase ไม่มีขีดล่าง (`leave_requests` → `leaveRequests`)

## 2. สถานะใบลา — มี 3 ค่าเท่านั้น

- `รอพิจารณา` — ค่าเริ่มต้นตอนสร้างใบใหม่
- `อนุมัติ`
- `ไม่อนุมัติ`

เปลี่ยนได้ทางเดียวจาก `รอพิจารณา` ไปเป็น `อนุมัติ` หรือ `ไม่อนุมัติ` เท่านั้น ห้ามย้อนกลับ และใบที่พิจารณาแล้วเปลี่ยนสถานะต่อไม่ได้

## 3. ข้อห้าม: ห้ามใส่คีย์ลงไฟล์ที่ push

ห้ามเพิ่มหรือปล่อยให้ API key, service account key, หรือค่าลับอื่น ๆ อยู่ในไฟล์ที่จะ commit/push ขึ้น GitHub

ก่อน push ทุกครั้ง ให้ตรวจรายการไฟล์ที่กำลังจะขึ้น GitHub และเช็คเนื้อไฟล์ที่มีคำว่า key / secret / config ด้วยตาก่อนเสมอ (ดูหัวข้อท้าย `README.md`)
