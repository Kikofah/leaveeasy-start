# ACL.md — ตารางสิทธิ์ผู้ใช้ 3 บทบาทของ LeaveEasy

อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 — ถ้าแก้สิทธิ์ ให้แก้ที่สเปกก่อนแล้วค่อยแก้ไฟล์นี้ตาม

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลา · ดูใบลาของคนอื่น · แก้ประเภทการลา |
| ✅ **ผู้อนุมัติ** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ / ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ประเภทการลา · **อนุมัติใบลาของตัวเอง** (ต้องให้ผู้อนุมัติคนอื่นหรือฝ่ายบุคคลพิจารณาแทน) |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม แก้ ลบ ประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | **อนุมัติใบลาของตัวเอง** (กฎเดียวกับผู้อนุมัติ ใช้ตอน hr ยื่นใบลาของตัวเอง) |

## สถานะการบังคับใช้จริงในโค้ดตอนนี้ (อัปเดตหลัง deploy ขึ้น Firebase Hosting จริง)

**บังคับแล้วครบตามตาราง:**
- 🙋 **ผู้ขอลา (`employee`):** เห็นแค่ใบลาของตัวเอง (กรองตั้งแต่ Firestore query ใน `js/leave-requests.js` ไม่ใช่แค่ซ่อนที่หน้าจอ) · เปิดดูใบลาของคนอื่นไม่ได้ (`js/leave-request-detail.js`) · ไม่เห็นปุ่มอนุมัติ/ไม่อนุมัติเลยไม่ว่าใบใคร · ลบได้เฉพาะใบตัวเองตอนสถานะ `รอพิจารณา`
- ✅ **ผู้อนุมัติ (`manager`):** เห็นใบลาทุกใบ · อนุมัติ/ไม่อนุมัติได้ทุกใบยกเว้นใบของตัวเอง · ลบได้เฉพาะใบตัวเอง
- 🧾 **ฝ่ายบุคคล (`hr`):** เห็น/อนุมัติ/ลบได้ทุกใบ (ยกเว้นอนุมัติใบตัวเอง) · เป็น role เดียวที่เข้าเมนูและหน้า "ประเภทการลา" ได้ (role อื่นไม่เห็นเมนูนี้ และเจอข้อความปฏิเสธถ้าพิมพ์ URL เข้าตรงๆ)
- **Firestore Security Rules deploy แล้วจริง** (`firestore.rules`) — กฎเดียว: ต้องล็อกอินก่อนถึงอ่าน/เขียนได้ทุกโฟลเดอร์ บังคับที่ระดับฐานข้อมูลจริงแล้ว ไม่ใช่แค่ฝั่งหน้าเว็บอีกต่อไป
- เว็บ deploy ขึ้น Firebase Hosting จริงที่ **https://leaveeasy-witchudakhamsom.web.app**
- **`dashboard.html` มีแล้ว** — นับตัวเลขสถานะและรายการล่าสุดจากข้อมูลจริงใน Firestore (นอกเหนือสเปก Module 2 ทำเพิ่มตามที่ขอ) เห็นเฉพาะข้อมูลที่ role นั้นมีสิทธิ์เห็น (employee เห็นแค่ของตัวเอง เหมือนหน้ารายการใบลา)

**ยังไม่ครบ / เป็นข้อจำกัดที่เหลืออยู่:**
- **role-based ยังไม่มีใน Security Rules** — กฎที่ deploy ไปมีแค่ "ต้องล็อกอิน" การจำกัดตาม role ทั้งหมดในตารางนี้ยังเป็นแค่ฝั่งหน้าเว็บ (JavaScript) เท่านั้น ถ้ามีคนเรียก Firestore ตรงๆ ข้ามหน้าเว็บ (เช่น เปิด DevTools เขียนโค้ดเอง) ก็ยังข้ามข้อจำกัดพวกนี้ได้ ตราบใดที่ล็อกอินอยู่
- **"กำหนดผู้อนุมัติให้แต่ละใบ" ของ hr — ยังไม่มีหน้าไหนทำได้เลย** ใบลาใหม่ทุกใบถูกสร้างด้วย `approverId` ว่างเสมอ ไม่มี UI ให้ hr เลือกผู้อนุมัติ
- **"เพิ่ม แก้ ลบ ประเภทการลา" ของ hr — เข้าถึงหน้าได้แล้ว (เฉพาะ hr) แต่ตัวฟังก์ชันในหน้านั้นยังเป็นข้อมูลปลอมในหน่วยความจำ** (`js/leave-types.js` ยังใช้ `window.LEAVE_DATA` ไม่ได้เขียนลง Firestore จริง) กด F5 แล้วข้อมูลที่แก้จะหายเหมือนเดิม

> 📌 กฎ "ห้ามอนุมัติใบลาของตัวเอง" ยังเป็นกฎที่เพิ่มเข้ามาเอง ไม่ได้อยู่ใน `leaveeasy-spec.md` หัวข้อ 2 — ถ้าต้องการให้เป็นทางการ ควรเพิ่มลงสเปกด้วย ไม่ใช่แค่ไฟล์นี้
