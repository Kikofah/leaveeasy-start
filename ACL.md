# ACL.md — ตารางสิทธิ์ผู้ใช้ 3 บทบาทของ LeaveEasy

อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 — ถ้าแก้สิทธิ์ ให้แก้ที่สเปกก่อนแล้วค่อยแก้ไฟล์นี้ตาม

| บทบาท | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | ยื่นใบขอลาใหม่ · ดูใบลาของตัวเอง · เขียนความเห็นเพิ่มในใบของตัวเอง · ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | เปลี่ยนสถานะใบลา · ดูใบลาของคนอื่น · แก้ประเภทการลา |
| ✅ **ผู้อนุมัติ** (`manager`) | ดูใบลาทุกใบ · เปลี่ยนสถานะใบลา (อนุมัติ / ไม่อนุมัติ) · เขียนความเห็นในทุกใบ | ลบใบลาของคนอื่น · แก้ประเภทการลา · **อนุมัติใบลาของตัวเอง** (ต้องให้ผู้อนุมัติคนอื่นหรือฝ่ายบุคคลพิจารณาแทน) |
| 🧾 **ฝ่ายบุคคล** (`hr`) | ทำได้ทุกอย่างที่ผู้อนุมัติทำได้ · กำหนดผู้อนุมัติให้แต่ละใบ · เพิ่ม แก้ ลบ ประเภทการลา · เห็นแดชบอร์ดสรุปทั้งระบบ | **อนุมัติใบลาของตัวเอง** (กฎเดียวกับผู้อนุมัติ ใช้ตอน hr ยื่นใบลาของตัวเอง) |

## สถานะการบังคับใช้จริงในโค้ดตอนนี้

- **บังคับแล้ว:** ผู้ขอลา (`employee`) เปิดใบลาของคนอื่นไม่ได้ (เช็คใน `js/leave-request-detail.js` จาก `role` ที่อ่านจากโฟลเดอร์ `users`)
- **ยังไม่บังคับ:** ข้อจำกัดที่เหลือทั้งหมดในตาราง (เช่น ห้ามผู้ขอลาเปลี่ยนสถานะ, ห้ามแก้ประเภทการลาถ้าไม่ใช่ฝ่ายบุคคล, **ห้ามอนุมัติใบลาของตัวเอง**) ยังเป็นแค่ปุ่ม/หน้าที่เปิดให้ทุกคนที่ล็อกอินแล้วกดได้เหมือนกันหมด — ยังไม่มีการเช็ค `role` ก่อนแสดงปุ่มหรือก่อนเขียน Firestore
- **ยังไม่มี Security Rules** กันไว้ระดับฐานข้อมูลเลย ตอนนี้ใครก็เรียก Firestore ตรงๆ ข้ามหน้าเว็บมาแก้ข้อมูลได้ทั้งหมด ไม่ว่าจะมี role อะไร

> 📌 **กฎ "ห้ามอนุมัติใบลาของตัวเอง" เป็นกฎที่เพิ่มเข้ามาใหม่ ไม่ได้อยู่ใน `leaveeasy-spec.md` หัวข้อ 2** — ถ้าต้องการให้เป็นกฎทางการของระบบ ควรเพิ่มลงสเปกด้วย ไม่ใช่แค่ไฟล์นี้

ตารางสิทธิ์นี้จึงเป็น "เป้าหมายที่ต้องทำให้ครบ" มากกว่าสภาพจริงตอนนี้ทั้งหมด
